export type WasmInterface = {
	getAllResources: () => { graphics: { background: { data: { type: "Uint8Array", ptr: number, len: number }, width: number, height: number }, machine: { data: { type: "Uint8Array", ptr: number, len: number }, width: number, height: number }, chamber_increase: { sprite_sheet: { data: { type: "Uint8Array", ptr: number, len: number }, width: number, height: number }, animation_data: { framerate: number, frames: number[][] } }, ghost_idle_side: { sprite_sheet: { data: { type: "Uint8Array", ptr: number, len: number }, width: number, height: number }, animation_data: { framerate: number, frames: number[][] } }, ghost_idle_front: { sprite_sheet: { data: { type: "Uint8Array", ptr: number, len: number }, width: number, height: number }, animation_data: { framerate: number, frames: number[][] } }, ghost_idle_back: { sprite_sheet: { data: { type: "Uint8Array", ptr: number, len: number }, width: number, height: number }, animation_data: { framerate: number, frames: number[][] } }, ghost_fixing: { sprite_sheet: { data: { type: "Uint8Array", ptr: number, len: number }, width: number, height: number }, animation_data: { framerate: number, frames: number[][] } } }, config: { screen: { width: number, height: number }, controller_dead_zone: number, player_speed: number, fix_proximity: number, fix_snap_offset: { x: number, y: number }, chamber_location: { x: number, y: number }, machine_locations: { x: number, y: number }[] } }, 
	update: (arg0: { time_ms: number, keyboard: { left: boolean, right: boolean, up: boolean, down: boolean, interact: boolean }, joystick: { x: number, y: number, interact: boolean } }) => { last_time_ms: number, player: { x: number, y: number, direction: "Left" | "Right" | "Up" | "Down", action: "Idle" | "Fixing" } }, 
}